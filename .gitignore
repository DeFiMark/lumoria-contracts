# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hardhat
artifacts/
cache/
coverage/
coverage.json
typechain/
typechain-types/

# Secrets / env
.env
.env.*
!.env.example

# Ephemeral deployment artifacts (keep bsc.json / bscTestnet.json committed).
deployments/hardhat.json
deployments/localhost.json

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code session data + worktrees (local only — never commit)
.claude/
